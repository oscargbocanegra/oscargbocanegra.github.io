# 003 — Deep Architecture QA conformance remediation

## Decision

Add the audit-required metrics, six engagement practices, commitment models, and architecture-review CTA as bilingual YAML-backed components within `docs/`.

## Rationale

The QA audit identified missing above-the-fold quantitative proof, incomplete service coverage, absent engagement models, and a missing architecture-review conversion path. The remediation keeps the site native to Jekyll and makes each repeated block data-driven.

## Evidence boundary

The metric values and service claims are owner-supplied in the execution artifacts for this remediation. They should remain subject to the site's normal evidence review before being treated as independently verified public outcomes.